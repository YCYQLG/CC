# ── 前端 Service（集群内访问；任务5 验证用 exec/port-forward 即可）──
apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
